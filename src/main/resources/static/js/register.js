/**
 * 
 */

$(document).ready(function(){
//	let county_label_el = '<label>縣市:</label>';	
//	$("select.county").before(county_label_el);
//	
//	let district_label_el = '<label>地區:</label>';
//	$("select.district").before(district_label_el);
})
